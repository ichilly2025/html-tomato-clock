# FocusFlow 番茄钟应用

一个简洁美观的番茄钟应用，帮助您通过25分钟专注和5分钟休息的循环提高工作效率。

## 功能特性

- 25分钟工作模式和5分钟休息模式
- 直观的倒计时显示
- 开始、暂停、重置控制
- 一键切换工作/休息模式
- 计时结束提醒对话框
- 深色主题配合深橙色主题色

## 运行要求

- Flutter SDK 3.0.0 或更高版本
- Dart SDK 3.0.0 或更高版本

## 运行方法

### 快速启动脚本

**推荐：在Chrome浏览器中运行（最简单）**
```bash
# 1. 安装依赖
flutter pub get

# 2. 在Chrome浏览器中运行
flutter run -d chrome
```

**或者运行到其他设备**
```bash
# 1. 检查Flutter环境
flutter doctor

# 2. 安装依赖
flutter pub get

# 3. 运行应用（会提示选择设备）
flutter run
```

### 详细步骤

1. **检查Flutter环境**
   ```bash
   flutter doctor
   ```
   确保Flutter SDK已正确安装且版本 >= 3.0.0

2. **安装项目依赖**
   ```bash
   flutter pub get
   ```
   
3. **连接设备（可选）**
   - **Web浏览器（推荐）**: 无需额外设置，直接运行即可
   - iOS: 启动iOS模拟器或连接iPhone
   - Android: 启动Android模拟器或连接Android设备
   - macOS/Windows/Linux: 在对应平台运行

4. **运行应用**
   ```bash
   # 推荐：直接在Chrome浏览器运行（无需设备）
   flutter run -d chrome
   
   # 或让Flutter提示选择设备
   flutter run
   
   # 或指定其他平台运行
   flutter run -d macos         # macOS桌面应用
   flutter run -d ios           # iOS模拟器/设备
   flutter run -d android       # Android模拟器/设备
   ```

### 浏览器运行说明（推荐新手）

如果你是第一次运行Flutter项目，**强烈推荐使用Chrome浏览器**，因为：
- ✅ 无需安装模拟器
- ✅ 无需连接真机设备
- ✅ 启动速度快
- ✅ 方便调试和预览

运行命令：
```bash
flutter pub get
flutter run -d chrome
```

应用会自动在Chrome浏览器中打开，你可以立即看到番茄钟界面。

### 一键启动脚本

创建启动脚本（可选）：

**macOS/Linux（浏览器版）** - 创建 `start.sh`：
```bash
#!/bin/bash
echo "🍅 启动 FocusFlow 番茄钟..."
flutter pub get && flutter run -d chrome
```

**macOS/Linux（自动选择设备）** - 创建 `start.sh`：
```bash
#!/bin/bash
echo "🍅 启动 FocusFlow 番茄钟..."
flutter pub get && flutter run
```

运行权限并执行：
```bash
chmod +x start.sh
./start.sh
```

**Windows（浏览器版）** - 创建 `start.bat`：
```batch
@echo off
echo 启动 FocusFlow 番茄钟...
flutter pub get
flutter run -d chrome
```

## 项目结构

- `lib/main.dart` - 主应用文件，包含所有核心逻辑
- `pubspec.yaml` - 项目依赖配置

## 设计说明

- 采用Material Design 3设计规范
- 深色主题模式，减少视觉疲劳
- 深橙色主题色，提升视觉吸引力
- 响应式布局，适配不同屏幕尺寸

## 技术特点

- 使用Flutter原生setState进行状态管理
- 无复杂第三方依赖
- 代码简洁，注释清晰
- 符合Dart最佳实践