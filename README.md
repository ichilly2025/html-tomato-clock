# FocusFlow 番茄钟应用

一个简洁美观的番茄钟应用，帮助您通过25分钟专注和5分钟休息的循环提高工作效率。

## 功能特性

- 25分钟工作模式和5分钟休息模式
- 直观的倒计时显示
- 开始、暂停、重置控制
- 一键切换工作/休息模式
- 计时结束提醒对话框
- 深色主题配合深橙色主题色

## 运行要求

- Flutter SDK 3.0.0 或更高版本
- Dart SDK 3.0.0 或更高版本

## 运行方法

### 快速启动脚本

**推荐：在Chrome浏览器中运行（最简单）**
```bash
# 1. 安装依赖
flutter pub get

# 2. 在Chrome浏览器中运行
flutter run -d chrome
```

**或者运行到其他设备**
```bash
# 1. 检查Flutter环境
flutter doctor

# 2. 安装依赖
flutter pub get

# 3. 运行应用（会提示选择设备）
flutter run
```

### 详细步骤

1. **检查Flutter环境**
   ```bash
   flutter doctor
   ```
   确保Flutter SDK已正确安装且版本 >= 3.0.0

2. **安装项目依赖**
   ```bash
   flutter pub get
   ```
   
3. **连接设备（可选）**
   - **Web浏览器（推荐）**: 无需额外设置，直接运行即可
   - iOS: 启动iOS模拟器或连接iPhone
   - Android: 启动Android模拟器或连接Android设备
   - macOS/Windows/Linux: 在对应平台运行

4. **运行应用**
   ```bash
   # 推荐：直接在Chrome浏览器运行（无需设备）
   flutter run -d chrome
   
   # 或让Flutter提示选择设备
   flutter run
   
   # 或指定其他平台运行
   flutter run -d macos         # macOS桌面应用
   flutter run -d ios           # iOS模拟器/设备
   flutter run -d android       # Android模拟器/设备
   ```

### 浏览器运行说明（推荐新手）

如果你是第一次运行Flutter项目，**强烈推荐使用Chrome浏览器**，因为：
- ✅ 无需安装模拟器
- ✅ 无需连接真机设备
- ✅ 启动速度快
- ✅ 方便调试和预览

运行命令：
```bash
flutter pub get
flutter run -d chrome
```

应用会自动在Chrome浏览器中打开，你可以立即看到番茄钟界面。

### 一键启动脚本

创建启动脚本（可选）：

**macOS/Linux（浏览器版）** - 创建 `start.sh`：
```bash
#!/bin/bash
echo "🍅 启动 FocusFlow 番茄钟..."
flutter pub get && flutter run -d chrome
```

**macOS/Linux（自动选择设备）** - 创建 `start.sh`：
```bash
#!/bin/bash
echo "🍅 启动 FocusFlow 番茄钟..."
flutter pub get && flutter run
```

运行权限并执行：
```bash
chmod +x start.sh
./start.sh
```

**Windows（浏览器版）** - 创建 `start.bat`：
```batch
@echo off
echo 启动 FocusFlow 番茄钟...
flutter pub get
flutter run -d chrome
```

## 构建Web版本（用于部署到服务器）

如果你想将应用部署到服务器或分享给他人，需要构建生产版本的Web文件。

### 完整构建步骤

#### 1. 安装依赖
```bash
flutter pub get
```
下载并安装项目所需的所有Flutter包。

#### 2. 构建Web生产版本
```bash
flutter build web
```

这个命令会：
- ✅ 编译Dart代码为优化后的JavaScript
- ✅ 压缩和优化所有资源文件
- ✅ 生成PWA（渐进式Web应用）支持文件
- ✅ 优化字体和图标（Tree-shaking）
- ✅ 输出生产就绪的静态文件

构建完成后，输出信息：
```
✓ Built build/web
```

#### 3. 查看构建产物

所有Web文件位于：
```
build/web/
```

目录结构：
```
build/web/
├── index.html                    # 主HTML文件（入口）
├── main.dart.js                  # 编译后的Dart代码（~1.8MB）
├── flutter.js                    # Flutter Web运行时
├── flutter_bootstrap.js          # 启动脚本
├── flutter_service_worker.js     # Service Worker（离线支持）
├── manifest.json                 # Web应用清单（PWA配置）
├── version.json                  # 版本信息
├── favicon.png                   # 网站图标
├── assets/                       # 资源文件夹（字体、图片等）
├── canvaskit/                    # Flutter渲染引擎
└── icons/                        # 应用图标
```

#### 4. 部署到服务器

**方法1：直接拷贝**
```bash
# 将build/web目录下的所有文件拷贝到服务器Web目录
cp -r build/web/* /path/to/your/server/public/
```

**方法2：打包上传**
```bash
# 打包成zip文件
cd build
zip -r focusflow-web.zip web/

# 或打包成tar.gz
tar -czf focusflow-web.tar.gz web/

# 然后上传到服务器并解压
```

**方法3：使用部署脚本（可选）**

创建 `deploy.sh`：
```bash
#!/bin/bash
echo "📦 构建Web版本..."
flutter pub get
flutter build web

echo "🗜️  打包文件..."
cd build
tar -czf focusflow-web.tar.gz web/

echo "✅ 部署包已生成: build/focusflow-web.tar.gz"
echo "📤 请将此文件上传到服务器并解压到Web根目录"
```

运行：
```bash
chmod +x deploy.sh
./deploy.sh
```

#### 5. 服务器配置

将 `build/web/` 目录下的所有文件上传到服务器的Web根目录，例如：
- **Nginx**: `/usr/share/nginx/html/` 或 `/var/www/html/`
- **Apache**: `/var/www/html/`
- **静态托管服务**: Vercel, Netlify, GitHub Pages等

确保服务器配置支持：
- ✅ 静态文件服务
- ✅ SPA（单页应用）路由支持（可选）
- ✅ HTTPS（推荐）

#### 6. 本地预览构建结果

构建完成后，可以在本地运行查看效果。

**方法1：使用Python HTTP服务器（推荐，无需安装）**

```bash
# 进入构建目录
cd build/web

# 启动HTTP服务器（端口8000）
python3 -m http.server 8000
```

启动成功后会显示：
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

**访问应用**：在浏览器打开
- **http://localhost:8000**
- 或 **http://127.0.0.1:8000**

**停止服务器**：在终端按 `Ctrl + C`

---

**方法2：使用PHP服务器**

```bash
cd build/web
php -S localhost:8000
```

访问：**http://localhost:8000**

---

**方法3：使用Node.js serve工具**

```bash
# 首次需要安装serve
npm install -g serve

# 启动服务器
serve -s build/web -p 8000
```

访问：**http://localhost:8000**

---

**方法4：直接打开HTML（不推荐）**

```bash
# macOS
open build/web/index.html

# Linux
xdg-open build/web/index.html

# Windows
start build/web/index.html
```

⚠️ **注意**：直接打开HTML文件可能因为浏览器CORS策略导致部分功能异常，**建议使用HTTP服务器方式运行**。

---

**完整流程示例**

```bash
# 1. 构建生产版本
flutter build web

# 2. 进入构建目录并启动服务器
cd build/web && python3 -m http.server 8000

# 3. 在浏览器访问 http://localhost:8000
```

### 快速构建和部署命令

```bash
# 完整流程：安装依赖 → 构建 → 预览
flutter pub get && flutter build web && cd build/web && python3 -m http.server 8000
```

### 构建选项（高级）

```bash
# 构建并启用WebAssembly（实验性，性能更好）
flutter build web --wasm

# 指定构建模式
flutter build web --release          # 生产模式（默认）
flutter build web --profile          # 性能分析模式

# 自定义输出路径
flutter build web --output=dist/

# 禁用Tree-shaking（保留所有字体图标）
flutter build web --no-tree-shake-icons
```

## 项目结构

- `lib/main.dart` - 主应用文件，包含所有核心逻辑
- `pubspec.yaml` - 项目依赖配置

## 设计说明

- 采用Material Design 3设计规范
- 深色主题模式，减少视觉疲劳
- 深橙色主题色，提升视觉吸引力
- 响应式布局，适配不同屏幕尺寸

## 技术特点

- 使用Flutter原生setState进行状态管理
- 无复杂第三方依赖
- 代码简洁，注释清晰
- 符合Dart最佳实践